# frozen_string_literal: true

RSpec.describe WordGameSolver do
  it "has a version number" do
    expect(WordGameSolver::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
